## .bashrc

```bash
# двойная строка и подсветка ветки git
git_branch() {
  git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'
}
PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\
\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[01;33m\
$(git_branch)\n\[\033[00m\]\[\033[0;31m\]\$\[\033[0;33m\] '


#======== my aliases

alias composer='php /usr/local/bin/composer'

# git
alias gs='git status'
alias ga='git add .'
alias gc='git commit -m $*'
alias gaf='git add $*'
alias gaa='git add -A'
alias gpl='git pull'
alias gps='git push'
alias gca='git commit -a $*'
alias gcam='git commit -am $*'
alias gch='git checkout $*'
alias gchf='git checkout -f'
alias gbr='git branch'
alias gl='git log'
alias glpo='git log --pretty=oneline'
alias glpon='git log --pretty=oneline -n $*'


# docker
alias d_up='docker-compose up -d'
alias dd='docker-compose down'

# old portal:
alias d01='docker-compose down; docker-compose up -d; docker exec -it cot_app bash'
alias d02='docker exec -it cot_app bash'
alias d03='docker exec -it cot_db bash'
alias d04='docker exec -it cot_nginx bash'

# new portal:
alias d0='docker-compose down; docker-compose up -d; docker-compose exec portal bash'
alias d1='docker-compose exec portal bash'
alias d2='docker-compose exec db bash'
alias d3='docker-compose exec nginx bash'
alias d4='docker-compose exec redis bash'
alias cd1='cd ~/_elcom/portal/'

# laravel
alias pa='php artisan $*'
alias paclear='php artisan cache:clear && php artisan view:clear && php artisan route:clear && php artisan config:clear && php artisan clear-compiled'
alias sail='[ -f sail ] && sh sail || sh vendor/bin/sail'

alias m0='php artisan make:migration $'
alias m1='php artisan migrate'
alias m2='php artisan migrate:rollback'

# other
alias n1='npm run watch'
alias aliases='tail -n 45 ~.bashrc'
alias h='history'
alias c='clear'
alias up='sudo apt update && sudo apt upgrade'
alias scode='sudo code --no-sandbox --user-data-dir ~/supercode'
alias psnginx='echo "command: ps -ax | grep nginx"; ps -ax | grep nginx'
alias nanobash='sudo nano ~/.bashrc'
alias source='source ~/.bashrc'
```