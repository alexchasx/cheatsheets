sudo apt install dos2unix 
find -type f -print0 | xargs -0 dos2unix 
-type f		# искать только файлы
-print0		# 

 "-print0 True; выведите полное имя файла в стандартном выводе, за которым следует нулевой символ (вместо символа новой строки, который использует -print ). Это позволяет правильно интерпретировать имена файлов, содержащие новые строки или другие типы пробелов, программами, которые обрабатывают вывод find . Этот параметр соответствует параметру -0 в xargs. "В этом случае xargsобрабатывается findвывод и print0гарантируется, что он правильно обрабатывает даже файлы, названные мусором, например символом новой строки