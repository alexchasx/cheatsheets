
## Сброс автоинкремента
```php
DB::statement("SELECT setval('trud_questions_id_seq', (SELECT MAX(id) from \"trud_questions\"));");

// или (хуже вариант; 16 - последний максимальный ID + 1)
DB::statement('ALTER SEQUENCE p_request_history_statuses_id_seq RESTART WITH 16');
```